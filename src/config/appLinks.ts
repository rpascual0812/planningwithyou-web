/** App URLs — use only on explicit user actions (e.g. pricing CTAs), not for automatic navigation. */
export const APP_ORIGIN = 'https://app.planningwithyou.com'
export const APP_SIGN_IN_URL = `${APP_ORIGIN}/login`
export const APP_REGISTER_URL = `${APP_ORIGIN}/register`
